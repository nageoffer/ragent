name: Java Static Analysis

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Set up JDK 17 with Maven cache
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Run static analysis tools
        run: |
          mvn clean verify -DskipTests

      - name: Upload Checkstyle HTML report
        uses: actions/upload-artifact@v4
        with:
          name: checkstyle-report-html
          path: target/reports/checkstyle.html  # 上传 Checkstyle HTML 格式报告
          retention-days: 7
          if-no-files-found: warn

#      - name: Upload PMD report
#        uses: actions/upload-artifact@v4
#        with:
#          name: pmd-report
#          path: target/pmd.xml  # 确保 PMD 报告路径正确
#          retention-days: 7
#          if-no-files-found: warn
#
#      - name: Upload SpotBugs report (XML)
#        uses: actions/upload-artifact@v4
#        with:
#          name: spotbugs-report-xml
#          path: target/spotbugs/spotbugs-xml.xml  # 确保 SpotBugs XML 报告路径正确
#          retention-days: 7
#          if-no-files-found: warn
#
#      - name: Upload SpotBugs report (HTML)
#        uses: actions/upload-artifact@v4
#        with:
#          name: spotbugs-report-html
#          path: target/spotbugs/spotbugs.html  # 确保 SpotBugs HTML 报告路径正确
#          retention-days: 7
#          if-no-files-found: warn